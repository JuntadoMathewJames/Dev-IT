<p><strong>Date of Expense: </strong><%= expenseDate %></p>
<table class="table table-hover table-bordered">
  <thead>
    <tr>
      <th>Description</th>
      <th>Amount</th>
      <th>Actions</th>
    </tr>
  </thead>
  <tbody>
    <% expenses.each do |expense| %>
      <tr>
        <td><%= expense.description%></td>
        <td><%= expense.amount%></td>
        <td>
          <%= link_to  "Delete", "/sales/expense/#{sale.id}/#{expense.id}/destroy", class:"btn btn-danger", data: { turbo_method: :delete} %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
